
Граница = "----Boundary" + Строка(Новый УникальныйИдентификатор);

ДвоичныеДанные = Новый ДвоичныеДанные(ПутьКФайлу);
ИмяФайла = "";
Категория = "";

ЗаписьПотока = Новый ЗаписьДанных(, КодировкаТекста.UTF8);

// поле file
ЗаписьПотока.ЗаписатьСтроку("--" + Граница + Символы.ВК + Символы.ПС);
ЗаписьПотока.ЗаписатьСтроку("Content-Disposition: form-data; name=""file""; filename=""" + ИмяФайла + """" + Символы.ВК + Символы.ПС);
ЗаписьПотока.ЗаписатьСтроку("Content-Type: application/octet-stream" + Символы.ВК + Символы.ПС + Символы.ВК + Символы.ПС);
ЗаписьПотока.Записать(ДвоичныеДанные);
ЗаписьПотока.ЗаписатьСтроку(Символы.ВК + Символы.ПС);

// поле category
ЗаписьПотока.ЗаписатьСтроку("--" + Граница + Символы.ВК + Символы.ПС);
ЗаписьПотока.ЗаписатьСтроку("Content-Disposition: form-data; name=""category""" + Символы.ВК + Символы.ПС + Символы.ВК + Символы.ПС);
ЗаписьПотока.ЗаписатьСтроку(Категория + Символы.ВК + Символы.ПС);

ЗаписьПотока.ЗаписатьСтроку("--" + Граница + "--" + Символы.ВК + Символы.ПС);

Запрос = Новый HTTPЗапрос("/documents");
Запрос.Заголовки.Вставить("Content-Type", "multipart/form-data; boundary=" + Граница);
Запрос.УстановитьТелоИзДвоичныхДанных(ЗаписьПотока.Закрыть());

Ответ = Соединение.ОтправитьДляОбработки(Запрос);